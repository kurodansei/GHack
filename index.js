var stage = new Kinetic.stage({
	container: 'gamecontainer'
});

var layer = new Kinetic.layer();

var animations = {
	idle: [{}],
	run: [{}],
	jump: [{}]
};

var imageObj = new Image();

imageObj.onload = function(){
	var chero = new Kinetic.Sprite({
		//x-position,
		//y-position,
		//image name
		//default animation
		//possible animations
		//frame rate 
		//index no of default animation
	
		//Add the shape to the layer
		
		
		//Add the laer to the stage
		
		
		//start sprite animation
		
		
		//resume transition
	
	
	});

//imageObj.src = '/path/to/image'



};
